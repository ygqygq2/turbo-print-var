/**
 * 全局常量定义
 */

/**
 * 扩展 ID
 */
export const EXTENSION_ID = 'turbo-print-log';

/**
 * 扩展名称
 */
export const EXTENSION_NAME = 'Turbo Print Log';

/**
 * 配置前缀
 * 用于 vscode.workspace.getConfiguration(CONFIG_PREFIX)
 */
export const CONFIG_PREFIX = 'turbo-print-log';
